import { rentalPropertyType } from 'super-rentals-bootstrap/helpers/rental-property-type';
import { module, test } from 'qunit';

module('Unit | Helper | rental property type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rentalPropertyType([42]);
  assert.ok(result);
});
